
# Playwright
node_modules/
/test-results/
/test-reports/playwright-report/
/playwright-report/
test-reports/playwright-report/index.html
/blob-report/
/playwright/.cache/
/playwright/.auth/
allure-results/

# Environment & Credentials (NEVER commit credentials)
utils/environment.env
.env
.env.local
.env.*.local

# Local-only setup scripts (not needed in remote repo)
setup-jenkins.bat
setup.bat
setup.sh

# Local-only documentation (not needed in remote repo)
CI_CD_SECURITY_CONFIGURATION.md
JENKINS_SETUP_GUIDE.md

#git update-index --skip-worktree playwright.config.ts //to skip file from git track and git commit
#git update-index --no-skip-worktree playwright.config.ts //Git to track changes again
